/*
let nota1 = Number(prompt("Digite a nota do aluno"));
    let nota2 = Number(prompt("Digite a nota do aluno"));
    let nota3 = Number(prompt("Digite a nota do aluno"));
    let nota4 = Number(prompt("Digite a nota do aluno"));
    let quantNota = Number(prompt("De quantas notas deseja obter a média?"));

    let media = nota1 + nota2 + nota3 + nota4 / quantNota;

    if (media >= 6) {
        alert(`A média é igual á ${media.toFixed(1)}.APROVADO !!`);
    }
    else {
        alert(`A média é igual á ${media.toFixed(1)}.REPROVADO !!`);
}
        
let x = 3;
        x = 4 * ((3 + 3) ** 2 / 2 ) + x;
        console.log(x)
        */

        var dado1 = 1;  

        var dado2 = "1"; 
        
        var dado3 = 1; 
        
        if ( dado1 === dado2 || dado1 === dado3 )
            { console.log("As variáveis são estritamente iguais") 

            }